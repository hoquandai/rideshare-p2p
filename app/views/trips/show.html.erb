<h1 class="page-header">Trip Info</h1>
<h2 class="page-subheader">Trip #<%= @trip.id %></h2>
<%= render partial: "shared/table", locals: { table_trips: [@trip], is_allowed_to_rate: false } %>
<section class="options">
<%= link_to "Back", trips_path %>
<%= link_to "Delete", trip_path(@trip.id), method: :delete, data: {confirm: "You sure?"} %>
<%= link_to "Edit", edit_trip_path %>
</section>
