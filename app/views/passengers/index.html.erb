<h1 class="page-header">All Passengers</h1>
<ul class="user-list">
  <% @passengers.each do |passenger| %>
    <% next if passenger.is_deactivated %>
    <li><%= link_to passenger.name, passenger_path(passenger.id) %>: Phone: <%= passenger.phone_num %></li>
  <% end%>
</ul>
